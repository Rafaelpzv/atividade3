Aqui está um `README.md` completo e profissional para o seu projeto.

---

```markdown
# API Escola

API RESTful para gerenciamento de alunos e suas disciplinas, desenvolvida com Node.js, Express e seguindo boas práticas de organização em camadas.

## 📋 Funcionalidades

- Listar todos os alunos
- Buscar um aluno por RA
- Listar disciplinas de um aluno específico
- Criar um novo aluno (com validações)
- Atualizar dados de um aluno (nome e/ou disciplinas)
- Deletar um aluno
- Rota de documentação `/help`

## 🛠️ Tecnologias utilizadas

- [Node.js](https://nodejs.org/)
- [Express](https://expressjs.com/)
- [dotenv](https://github.com/motdotla/dotenv) (gerenciamento de variáveis de ambiente)

## 📁 Estrutura do projeto

```
.
├── .env
├── .gitignore
├── app.js
├── package.json
├── README.md
├── data/
│   └── alunosData.js
├── services/
│   └── alunoService.js
├── controllers/
│   └── alunoController.js
└── routes/
    └── alunos.js
```

## 🚀 Como executar o projeto

### Pré-requisitos

- Node.js (versão 14 ou superior)
- npm (geralmente já vem com o Node)

### Passos

1. **Clone o repositório** (ou crie os arquivos manualmente)

2. **Instale as dependências**
   ```bash
   npm install express dotenv
   ```

3. **Configure o ambiente**  
   Crie um arquivo `.env` na raiz com o conteúdo:
   ```env
   PORT=3000
   ```

4. **Inicie o servidor**
   ```bash
   node app.js
   ```
   ou, se configurou o script `start` no `package.json`:
   ```bash
   npm start
   ```

   Você verá a mensagem:
   ```
   API Escola rodando em http://localhost:3000
   Documentação disponível em http://localhost:3000/help
   ```

## 📌 Endpoints

| Método | Rota                           | Descrição                          |
|--------|--------------------------------|------------------------------------|
| GET    | `/alunos`                      | Lista todos os alunos              |
| GET    | `/alunos/:ra`                  | Busca aluno pelo RA                |
| GET    | `/alunos/:ra/disciplinas`      | Lista disciplinas de um aluno      |
| POST   | `/alunos`                      | Cria um novo aluno                 |
| PUT    | `/alunos/:ra`                  | Atualiza dados de um aluno         |
| DELETE | `/alunos/:ra`                  | Remove um aluno                    |
| GET    | `/help`                        | Exibe documentação interativa      |

### Exemplos de requisições

#### GET /alunos
```bash
curl http://localhost:3000/alunos
```

#### GET /alunos/1
```bash
curl http://localhost:3000/alunos/1
```

#### GET /alunos/1/disciplinas
```bash
curl http://localhost:3000/alunos/1/disciplinas
```

#### POST /alunos (criar aluno)
```bash
curl -X POST http://localhost:3000/alunos \
  -H "Content-Type: application/json" \
  -d '{"ra":"4","nome":"Ana","disciplinas":[{"codigo":"MAT101","nome":"Matemática","professor":"Prof. Carlos"}]}'
```

#### PUT /alunos/1 (atualizar nome)
```bash
curl -X PUT http://localhost:3000/alunos/1 \
  -H "Content-Type: application/json" \
  -d '{"nome":"João Silva"}'
```

#### DELETE /alunos/4
```bash
curl -X DELETE http://localhost:3000/alunos/4
```

## ✅ Validações implementadas

- Todos os campos obrigatórios (`ra`, `nome`, `disciplinas` com `codigo`, `nome`, `professor`) são verificados.
- Códigos de disciplina duplicados dentro do mesmo aluno não são permitidos.
- RA deve ser único na criação.
- Atualização parcial permitida (apenas `nome` ou apenas `disciplinas`).

## 📖 Ajuda interativa

Acesse `http://localhost:3000/help` no navegador para visualizar uma documentação em JSON com todos os endpoints e exemplos prontos.

## 📄 Licença

Este projeto foi desenvolvido para fins educacionais. Sinta-se à vontade para usá-lo como base para seus estudos.
```

---

Esse README fornece uma visão clara do projeto, instruções de instalação, exemplos de uso e documentação dos endpoints. Salve o conteúdo em um arquivo `README.md` na raiz do seu projeto.
